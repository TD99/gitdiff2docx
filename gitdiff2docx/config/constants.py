"""Constants and paths for GitDiff2Docx."""

import os

# The directory where the main script is located (repository root)
# Go up from gitdiff2docx package to repository root
SCRIPT_DIR = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
